#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
    charcter = 'A'
    charcter array or string = charrey[] = {'A','B','C','D'}
    int numberArray[] = {12,345,23,3452,2134}
    char stringArray[number of elements in the array][length of the word**should be based on the longest word**]
    */

    int i;
    char sArray[3][15], names[10];
/*
    for (i = 0; i < 3; i++)
    {
        printf("Enter of the name = ");
        gets(sArray);
    }

    for (i = 0; i < 3; i++)
    {
        printf("%s\t", sArray[i]);
    }

    for (i = 0; i < 3; i++)
    {
        printf("Enter of the name and surname");

        getline(scanf, names[10]);
    }

    for (i = 0; i < 3; i++)
    {
        printf("%d - %s\n", i, names[i]);
    }
*/
    return 0;
}